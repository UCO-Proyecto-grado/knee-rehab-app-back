service: entidades-primarias-api-aws

frameworkVersion: '3.40.0'

provider:
  name: aws
  runtime: python3.10
  stage: dev
  region: us-east-1
  environment:
    DATABASE_HOST: database.c476myam6hcq.us-east-1.rds.amazonaws.com
    DATABASE_PORT: 5432
    DATABASE_NAME: dbkneerehab
    DATABASE_USER: userkneerehab
    DATABASE_PASSWORD: LFS1ieqpA7bhuCK3W1Xo

functions:
  api:
    handler: entidades_primarias.app.main.handler
    events:
      - httpApi:
          path: /{proxy+}
          method: any

package:
  patterns:
    - '!node_modules/**'
    - '!.serverless/**'
    - '!.venv/**'
    - '!.git/**'
    - '!.vscode/**'
    - '!tests/**'
    - '!*.pyc'
    - '!__pycache__/**'
    - 'entidades_primarias/**'
    - 'requirements.txt'

plugins:
  - serverless-python-requirements

custom:
  pythonRequirements:
    dockerizePip: true
    zip: false
    slim: false
    pythonBin: python3

  serverless-offline:
    httpPort: 3000
    lambdaPort: 3002
    noPrependStageInUrl: true
